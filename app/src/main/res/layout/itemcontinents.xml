<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/yellow"
    android:layout_margin="12dp"
    android:padding="16dp">

    <ImageView
        android:id="@+id/img_continent"
        android:layout_width="120dp"
        android:layout_height="100dp"
        android:background="@color/teal_700"
        android:scaleType="fitXY"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_continent_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:textColor="@color/black"
        android:textSize="24sp"
        app:layout_constraintStart_toEndOf="@id/img_continent"
        app:layout_constraintTop_toTopOf="@id/img_continent"
        tools:text="name of continent" />

    <TextView
        android:id="@+id/tv_continent_square"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:textColor="@color/black"
        android:textSize="24sp"
        app:layout_constraintStart_toStartOf="@id/tv_continent_name"
        app:layout_constraintTop_toBottomOf="@id/tv_continent_name"
        tools:text="square" />

</androidx.constraintlayout.widget.ConstraintLayout>